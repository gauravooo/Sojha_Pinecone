name = "sojha-pinecone"

compatibility_date = "2024-04-05"
pages_build_output_dir = "dist"

[vars]
# Add non-sensitive environment variables here.
# NOTE: Secrets like RESEND_API_KEY must remain in .dev.vars for local development 
# and be added via the Cloudflare Pages UI for your production deployment.

[[d1_databases]]
binding = "DB"
database_name = "hotel-db"
database_id = "b3ae34ed-bd50-4957-b493-875410c8a7f3"

